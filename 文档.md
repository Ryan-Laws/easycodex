# EasyCodex 项目文档

EasyCodex 是一个本地优先的 Codex 控制台：桌面端负责启动和守护本机中转服务，安卓端通过认证 WebSocket 连接到中转服务，远程创建、恢复、观察和控制 Codex 任务。代码和密钥都留在用户自己的电脑上。

## 模块分工

| 模块 | 路径 | 说明 |
| --- | --- | --- |
| 安卓端 | `mobile/` | Kotlin + Jetpack Compose 原生应用，负责连接、任务列表、会话、CLI 控制台、文件/Git 查看、通知和设置。 |
| Agent Relay | `agent-relay/` | Node.js/TypeScript WebSocket 服务，负责鉴权、工作区安全、Codex 进程管理、线程读取、附件、Git、文件、通知和更新检查。 |
| 桌面中继 | `desktop-relay/` | Electron 应用，负责图形化启动/停止中转、显示二维码、配置工作区/端口/Codex 路径、检查更新和轻量托盘模式。 |
| 脚本 | `scripts/` | npm CLI 与本地启动辅助脚本。 |

## 当前能力

- 手机和桌面都可以查看运行中任务、活跃 Codex 线程和历史可恢复线程。
- 可以创建新 agent、恢复 Codex thread、停止/中断任务、归档任务。
- 支持模型、reasoning effort、service tier、cwd、approval policy 和 system prompt。
- 手机端可以发送文本、图片和文件附件；中转端会把附件写入工作区 `.easycodex-attachments/` 后传给 Codex。
- 支持计划模式：计划消息会弹出审阅，可选择优化计划或开始执行。
- 支持差异审阅：查看 Git 状态、完整 diff、单文件 diff、文件预览，并提交选中的文件。
- 支持 Codex 用户输入请求和审批请求，手机端能回复、批准或拒绝。
- 支持多窗口 Codex CLI：每个窗口运行一次 `codex exec`，可选 cwd、模型、reasoning、sandbox 和 `--skip-git-repo-check`。
- 支持项目选择器、受信工作区、Git worktree 列表和 relay-managed repos。
- 支持断线重连、stream replay、活动线程轮询、未读完成提醒和本地连接状态通知。
- 支持通知偏好、通知历史、客户端语言同步和可选 Expo 推送。
- 桌面端支持端口预览、回收已有 EasyCodex relay 进程、轻量托盘模式、打包更新和源码 checkout 更新。

## 数据流

```text
安卓端/桌面端 UI
  -> WebSocket action
Agent Relay
  -> codex app-server JSON-RPC
Codex runtime
  -> turn/item/thread events
Agent Relay
  -> stream envelopes
安卓端/桌面端 UI
```

核心原则是中转端显式翻译协议：Codex 的事件格式可以变化，但移动端尽量消费稳定的 EasyCodex action 和 message type。

## 工作区安全

中转端只允许访问 allowed workspace roots：

- `CODEX_CWD` 或进程启动目录
- `~/.easycodex/repos`
- 用户显式信任的自定义工作区
- Codex 桌面端可见工作区
- 已发现的 Git worktree

如果目录不在允许范围内，客户端必须先调用 `trust_workspace_root`。中转端会拒绝磁盘根目录、用户 profile 根目录、Desktop/Documents/Downloads 根目录、系统目录、Program Files、AppData 等高风险路径。

## 附件规则

- 单次最多 12 个附件。
- 单文件最大 12 MB。
- 文件名会被清理并加时间戳。
- 文件保存到当前 cwd 的 `.easycodex-attachments/`。
- 图片会作为 local image input 发送给 Codex。
- 普通文件会在 prompt 中以本地路径引用，方便 Codex 读取。

## Codex 线程

EasyCodex 同时维护两类任务：

- relay-managed agent：当前由 EasyCodex relay 启动并仍在内存中管理的 agent。
- resumable Codex thread：从 Codex 全局状态/线程列表读取，可恢复但不一定有 relay 进程。

任务列表会合并这两类来源，并根据 `codex/threads_changed`、`agents/changed`、轮询和 stream replay 刷新。归档会调用 Codex thread archive，并从手机端任务列表隐藏。

## 持久化位置

仓库内只保存源代码和文档。运行时配置和状态在用户目录：

```text
~/.easycodex/config.json
~/.easycodex/agents.json
~/.easycodex/repos/
~/.easycodex/desktop-relay.json
~/.easycodex/desktop-relay-runtime/
```

Codex 线程、桌面状态、归档状态和 queued follow-up 信息来自 `~/.codex/`。

## 开发命令

```powershell
# Agent Relay
Set-Location agent-relay
npm install
npm run build

# Desktop Relay
Set-Location desktop-relay
npm install
npm start

# Android
Set-Location mobile
gradle assembleDebug
```

## 文档维护规则

- 改 WebSocket action、payload、stream event 时，同步更新 `AGENT.md`。
- 改安卓/桌面端页面结构或核心交互时，同步更新 `View.md`。
- 改整体架构、运行方式、模块边界时，同步更新本文档和 `README.md`。
- 改 release、签名、CI smoke 时，同步更新 `RELEASE.md`。
